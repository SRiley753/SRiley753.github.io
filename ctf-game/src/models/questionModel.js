class QuestionModel {
    constructor(questionText, answer, clue) {
        this.questionText = questionText;
        this.answer = answer;
        this.clue = clue;
    }
}

export default QuestionModel;